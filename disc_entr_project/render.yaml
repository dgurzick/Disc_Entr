services:
  - type: web
    name: disc-entr
    runtime: python
    plan: free
    buildCommand: "./build.sh"
    startCommand: "gunicorn disc_entr_project.wsgi:application"
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "false"
      - key: PYTHON_VERSION
        value: "3.11.0"
